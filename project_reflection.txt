================================================================================
PROJECT REFLECTION - Diabetes Risk Prediction
================================================================================

WHAT WORKED WELL
--------------------------------------------------------------------------------
✓ Feature Selection Strategy
  • Simplified from 21+ features to 6 clinically relevant ones
  • Selected features (BMI, Age, GenHlth, HighBP, HighChol, PhysActivity) 
    balanced predictive power with user-friendly input
  • Made the web interface simple and accessible

✓ Model Comparison Approach
  • Compared Logistic Regression (baseline) vs Histogram GBDT (advanced)
  • Used validation set for objective model selection
  • Selected Histogram GBDT with ROC AUC of 0.815

✓ Strong Performance Metrics
  • ROC AUC: 0.815 (good discrimination ability)
  • Accuracy: 85% (correct predictions on test set)
  • Proper train/val/test split with stratification maintained class balance

✓ End-to-End Pipeline
  • Automated data download with fallback URLs
  • Clean preprocessing pipeline (missing value imputation, stratified splits)
  • Deployable web interface (FastAPI backend + HTML frontend)
  • Batch prediction capability via CLI

✓ Project Structure
  • Well-organized codebase (data, models, reports, src, web)
  • Reproducible with random seeds and metadata tracking


CHALLENGES FACED
--------------------------------------------------------------------------------
⚠ Class Imbalance Impact
  • Dataset likely imbalanced (low diabetes prevalence in population)
  • Reflected in F1 Score (0.26) despite high accuracy (0.85)
  • High accuracy driven by correctly predicting majority class (no diabetes)
  • Low Average Precision (0.45) indicates difficulty detecting positive cases
  
⚠ Precision-Recall Trade-off
  • Model prioritizes overall accuracy over precision for diabetes detection
  • May miss some true diabetes cases (lower recall) but minimizes false alarms
  • Appropriate for screening tool, but clinical deployment needs threshold tuning

⚠ Limited Feature Engineering
  • Used raw features without interactions (e.g., BMI × Age, HighBP × HighChol)
  • Could potentially improve performance with domain-specific feature engineering
  • Trade-off: Simplicity vs. complexity in web interface

⚠ Model Interpretation
  • Histogram GBDT chosen for performance, but less interpretable than Logistic Regression
  • Feature importance available, but exact decision logic is complex
  • Balancing interpretability vs. predictive power


UNEXPECTED INSIGHTS FROM RESULTS
--------------------------------------------------------------------------------
🔍 Histogram GBDT Outperformed Linear Model
  • Non-linear relationships in health data more important than expected
  • Gradient boosting captured complex interactions between features
  • Suggests diabetes risk is not simply additive (e.g., BMI alone)

🔍 Physical Activity Feature
  • PhysActivity included in simplified feature set
  • May have lower individual importance but contributes as part of ensemble
  • Lifestyle factors interact with clinical indicators

🔍 Calibration Behavior
  • Model shows calibration curve (probability calibration)
  • Important for risk prediction where probabilities have medical meaning
  • Helps in threshold selection for clinical decision-making

🔍 Simplicity vs. Performance Trade-off Validated
  • 6 features achieved strong ROC AUC (0.815) without complex features
  • Proves that well-selected, clinically relevant features can be sufficient
  • User-friendly approach doesn't sacrifice significant model quality

🔍 Dataset Quality Impact
  • BRFSS 2015 dataset (250k+ records) provided robust training
  • Large sample size helped model learn patterns reliably
  • Stratified splits maintained distribution across train/val/test

================================================================================
KEY TAKEAWAY FOR PRESENTATION
================================================================================
Successfully built a practical diabetes risk prediction tool that balances:
  • Model Performance: Strong ROC AUC (0.815) for clinical screening
  • User Experience: Simple 6-feature input suitable for web deployment
  • Technical Soundness: Proper ML pipeline, model comparison, evaluation

Main limitation: Class imbalance affects precision for positive class detection,
which is typical for medical screening applications and can be addressed with
threshold tuning based on clinical priorities.

================================================================================


